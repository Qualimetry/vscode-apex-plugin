public class SimplifyBooleanNonCompliant {
    Boolean ternary(Boolean flag) {
        return flag ? true : false;
    }
    Boolean multiLine(Boolean flag) {
        if (flag) {
            return true;
        } else {
            return false;
        }
    }
}
